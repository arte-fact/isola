name: python-uv
description: "Python 3 + uv"
version: "1.0.0"
provision:
  script: install.sh
  verify: "python3 --version && /root/.local/bin/uv --version"
paths:
  bin:
    - /home/sandbox/.local/bin
  copy:
    - from: /root/.local
      to: /home/sandbox/.local
