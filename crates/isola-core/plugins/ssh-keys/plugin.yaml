name: ssh-keys
description: "Share host SSH keys (read-only)"
version: "1.0.0"
layer: user
auto_detect:
  host_path: .ssh
provision:
  script: ~
paths:
  host_mount:
    - from: .ssh
      to: .ssh
      readonly: true
